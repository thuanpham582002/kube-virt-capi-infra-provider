apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: ghcr.io/thuanpham582002/cluster-api-kubevirt-controller-amd64:latest
        name: manager
